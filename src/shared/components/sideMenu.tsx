import { App } from "../../styles/navigations/sideMenu";
import { TouchableOpacity } from 'react-native';
import { useNavigation } from "@react-navigation/native";

const TopBar = () => {
  
  const navigation = useNavigation<any>();

  return (
    <App.conteiner>
    </App.conteiner>
  );
}


export default TopBar